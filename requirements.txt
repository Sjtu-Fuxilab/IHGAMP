# IHGAMP pipeline requirements
# Tested with Python 3.10/3.11/3.12 on Linux (Ubuntu 24.04) with CUDA 12.1.

# core scientific stack
numpy>=1.24,<3.0
pandas>=2.0
pyarrow>=14.0
scipy>=1.10
joblib>=1.3
scikit-learn>=1.3

# plotting
matplotlib>=3.7

# deep learning (NB03 feature extraction; NB12 OSFM optional GPU path)
torch>=2.1
torchvision>=0.16
open_clip_torch>=2.24

# whole-slide image I/O
# Requires the OpenSlide C library on the system, e.g. on Debian/Ubuntu:
#   sudo apt-get install -y openslide-tools libopenslide0
openslide-python>=1.3
opencv-python>=4.8

# data sources used by NB04 (UCSC Xena Toil RNA-seq)
xenaPython>=1.0

# Loeffler et al. (2024) HRD table is distributed as XLSX; needed by NB13
openpyxl>=3.1

# notebook execution environment
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.25
